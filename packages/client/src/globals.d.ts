/**
 * Build-time constant injected by Vite's `define` (see vite.config.ts).
 * Holds the client package.json version, used by the feedback widget so
 * submissions can be correlated to a build.
 */
declare const __APP_VERSION__: string
