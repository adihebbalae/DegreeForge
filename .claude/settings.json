{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel 2>/dev/null || echo .)\" && (npm run lint --silent --if-present 2>/dev/null || python -m flake8 --quiet 2>/dev/null || true) && echo '✓ lint'"
          }
        ]
      }
    ]
  }
}
